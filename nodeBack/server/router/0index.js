'use strict';

const accountRouter = require('./account-router');
const router = require('./router');
const userRouter = require('./0user-router');

module.exports = {
    accountRouter,
    router,
    userRouter,
};